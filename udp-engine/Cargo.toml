[package]
name = "udp-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
socket2 = { version = "0.6.5", features = ["all"] }
core_affinity = "0.8.3"
libc = "0.2"
hdrhistogram = "7.5.0"

[profile.release]
debug = true 
