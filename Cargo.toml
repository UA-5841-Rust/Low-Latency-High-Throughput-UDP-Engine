[package]
name = "udp_engine"
version = "0.1.0"
edition = "2024"
description = "A batched, SO_REUSEPORT UDP engine for Linux"
license = "MIT"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
